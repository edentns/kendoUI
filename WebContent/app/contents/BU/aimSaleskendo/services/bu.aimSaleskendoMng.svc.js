(function () {
    "use strict";

    /**
     * @ngdoc function
     * @name BU.aimSaleskendoMng.service : BU.aimSaleskendoMngSvc
     * 부서관리
     */
    angular.module("BU.aimSaleskendoMng.service")
        .factory("BU.aimSaleskendoMngSvc", ["APP_CONFIG", "$http", function (APP_CONFIG, $http) {
            return {
            };
        }]);
}());